import XCTest
@testable import SwiftPackage_Xcode12

final class SwiftPackage_Xcode12Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftPackage_Xcode12().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
